Delicate section separator — a hairline rule with a small center dot, lifted straight from the catalog pages.

```jsx
<Divider />
<Divider tone="gold" width="120px" />
<Divider node={false} />
```

Props: `width`, `node` (center dot), `tone` (`nude | gold`).
