Soft cream-filled form field with an uppercase label and optional hint, with a rose focus ring. Used in the booking/contact form.

```jsx
<Input label="Seu nome" placeholder="Como podemos te chamar?" />
<Input label="Mensagem" multiline rows={5} />
```

Props: `label`, `hint`, `multiline`, `rows`, plus all native input/textarea attributes.
